# BOLT - pinned runtime requirements (see BOLT_SPEC.md Section 11, Phase 0)

# Core numerics / data
numpy>=2.0,<3
pandas>=2.2,<3
scipy>=1.13
pyarrow>=16.0            # parquet engine for data/processed/panel.parquet
PyYAML>=6.0
python-dateutil>=2.9
python-dotenv>=1.0

# Feature engineering
networkx>=3.3            # correlation-graph eigenvector centrality (G2)

# Models
scikit-learn>=1.5        # LogReg, RandomForest, MLP, metrics, scalers
xgboost>=2.1             # gradient boosting (G3 - omitted by the base paper)
# LSTM and GRU are implemented from scratch in NumPy: no torch, no tensorflow.

# Explainability
shap>=0.46               # TreeExplainer / LinearExplainer (G4)

# Ingestion
requests>=2.32
urllib3>=2.2

# Sentiment
# Default backend is a deterministic finance-lexicon scorer: no heavy dependency,
# fully reproducible, no GPU. The transformer backend described in
# BOLT_Base_Paper_Answers Section 10 is OPTIONAL and pulls ~2.5 GB of torch:
#     pip install -r requirements-sentiment.txt
# then set features.sentiment.backend: "finbert" in config/default.yaml.

# Blockchain commitment layer (G5)
web3>=7.0
eth-account>=0.13
py-solc-x>=2.0           # solidity compilation for contracts/deploy.py

# Reporting
matplotlib>=3.9

# Tooling
pytest>=8.2
pytest-cov>=5.0
